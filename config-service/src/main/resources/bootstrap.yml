server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/castrohectora/Programa-Advisers.git
          searchPaths: config-data
  profiles:
    active: dev
  security:
    user:
      name: root
      password: pass