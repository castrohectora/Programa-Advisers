server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/castrohectora/Programa-Advisers
          searchPaths: config-data
          skipSslValidation: true
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: pass